@page "/not-found"
@layout MainLayout

<div class="card shadow-sm border-0 p-5 text-center my-5 bg-white">
    <div class="mb-3">
        <Icon Name="alert" Size="48" Class="text-warning" />
    </div>
    <h3 class="fw-bold text-dark">Página no encontrada</h3>
    <p class="text-muted">La sección a la que intentas acceder no existe o ha cambiado de dirección.</p>
    <div>
        <a href="" class="btn btn-primary rounded-pill px-4">Volver al Panel Principal</a>
    </div>
</div>